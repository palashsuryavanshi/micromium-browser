package com.example.data

/**
 * Manual configuration for sign-in. Fill these in from the Firebase console
 * (Project settings -> General) once a Firebase project exists:
 *
 * 1. Download google-services.json into app/ (enables all Firebase calls).
 * 2. Paste the OAuth 2.0 Web client ID below (enables the Google button).
 *
 * Everything compiles and runs with the defaults; unconfigured features
 * show an explanatory message instead of crashing.
 */
object AuthConfig {
    const val GOOGLE_WEB_CLIENT_ID: String = ""
}
